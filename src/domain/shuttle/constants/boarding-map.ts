/** 로컬 시각 기준, 이 시각(시) 이상이면 저녁 탑승 좌표를 우선한다. */
export const SHUTTLE_EVENING_BOARDING_START_HOUR_LOCAL = 17;

export const SHUTTLE_BOARDING_MAP_DEFAULT_ZOOM = 16;

/** 인터랙티브 지도 컨테이너 최소 높이(px). */
export const SHUTTLE_BOARDING_MAP_MIN_HEIGHT_PX = 256;
